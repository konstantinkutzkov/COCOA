"""Lightweight preprocessing wrappers used by feature extraction.

v1 (Commit 2) contains only the METIS wrapper. The Arjun wrapper will
land if/when we move to preprocessing approach (B) from the plan.
"""
